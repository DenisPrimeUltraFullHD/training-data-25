# training-data-25: Java Collections Performance Analysis

## Проект: Аналіз операцій з Java колекціями та вимірювання продуктивності

Цей проект демонструє роботу з різними Java колекціями (List, Queue, Set, Map) та проводить комплексний аналіз їх продуктивності.

## Структура проекту

### Основні класи

1. **BasicDataOperation.java** - Основний координатор для всіх операцій
2. **BasicDataOperationUsingList.java** - Операції з `Vector<Character>`
3. **BasicDataOperationUsingQueue.java** - Операції з `PriorityQueue<Character>`
4. **BasicDataOperationUsingSet.java** - Операції з `TreeSet<Character>`
5. **BasicDataOperationUsingMap.java** ⭐ - Операції з `LinkedHashMap` та `TreeMap` із аналізом продуктивності
6. **DataFileHandler.java** - Управління читанням/записом файлів
7. **PerformanceTracker.java** - Відстеження продуктивності операцій

## Ключові особливості

### List операції (BasicDataOperationUsingList)
- ✅ Пошук елементів у Vector
- ✅ Визначення мін/макс значень
- ✅ Сортування даних
- ✅ Вимірювання часу виконання операцій

### Queue операції (BasicDataOperationUsingQueue)
- ✅ Операції peek() та poll()
- ✅ Пошук у PriorityQueue
- ✅ Аналіз зміни розміру черги
- ✅ Профілювання продуктивності

### Map операції ⭐ НОВИНКА (BasicDataOperationUsingMap)
- ✅ **Java record-подібний клас Pet** для зберігання даних про домашніх тварин
- ✅ **Спеціалізований Comparator для сортування**:
  ```java
  private static final Comparator<Pet> PET_COMPARATOR = 
      Comparator.comparing(Pet::nickname)
                .thenComparing(Pet::species, Comparator.reverseOrder());
  ```
- ✅ Операції з LinkedHashMap (пошук, додавання, видалення, сортування)
- ✅ Операції з TreeMap (автоматичне сортування за Comparator)
- ✅ **Комплексний аналіз швидкості обробки даних**:
  - Тестування на 10,000 елементів
  - Порівняння HashMap, LinkedHashMap та TreeMap
  - Вимірювання операцій додавання та пошуку
  - Табличне представлення результатів

## Запуск програми

### List та Queue операції:
```bash
# Компіляція
javac *.java

# Запуск з пошуком символа
java BasicDataOperation "&"
```

### Map операції та аналіз швидкості:
```bash
# Запуск
java BasicDataOperationUsingMap
```

## Приклад виходу

```
========= Операції з LinkedHashMap =========
Початковий розмір LinkedHashMap: 10
...
=== Пари ключ-значення в LinkedHashMap ===
  Pet{nickname='Ажур', species='1'} -> Василь
  Pet{nickname='Балакун', species='2'} -> Ганна
...
=== АНАЛІЗ ШВИДКОСТІ ОБРОБКИ ДАНИХ ===

📊 Тестування з 10000 елементами

1️⃣  HashMap:
  [Performance] додавання 10000 елементів: 25115683 ns (25.116 ms)
  [Performance] пошук 5000 елементів: 5606267 ns (5.606 ms)

📈 ПОРІВНЯЛЬНА ТАБЛИЦЯ:
...
📌 ВИСНОВКИ:
• HashMap: найшвидший для додавання та пошуку O(1)
• LinkedHashMap: збереження порядку вставки з незначним уповільненням
• TreeMap: автоматичне сортування, але повільніше O(log n)
```

## Вимірювання продуктивності

Всі операції вимірюються за допомогою класу `PerformanceTracker`, який відстежує час виконання в наносекундах та мілісекундах:

```
[Performance] <operation_name>: <nanoseconds> ns (<milliseconds> ms)
```

## Java record реалізація

Замість класичного Java 14+ record, використана Java 11 сумісна реалізація:

```java
public static class Pet {
    private final String nickname;
    private final String species;
    
    public Pet(String nickname, String species) {
        this.nickname = nickname;
        this.species = species;
    }
    
    public String nickname() { return nickname; }
    public String species() { return species; }
    
    @Override
    public boolean equals(Object o) { ... }
    
    @Override
    public int hashCode() { ... }
    
    @Override
    public String toString() { ... }
}
```

## Компаратор для сортування

Використання Stream API та Comparator для гнучкого сортування:

```java
// Сортування LinkedHashMap за ключами за допомогою PET_COMPARATOR
linkedHashMap = linkedHashMap.entrySet().stream()
    .sorted(Map.Entry.comparingByKey(PET_COMPARATOR))
    .collect(Collectors.toMap(
        Map.Entry::getKey,
        Map.Entry::getValue,
        (e1, e2) -> e1,
        LinkedHashMap::new
    ));
```

## Технічні деталі

- **Версія Java**: 11+
- **Парадигма**: Об'єктно-орієнтована програмізація з функціональними елементами
- **API**: Java Collections Framework + Stream API
- **Профілювання**: Вимірювання часу за допомогою System.nanoTime()

## Автор та дата

Проект розроблений як навчальний матеріал з дослідження продуктивності Java колекцій.

---

**Для докладнішої інформації див. `IMPLEMENTATION_SUMMARY.md`**